# === COMMON === #

# binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# binaries
bin/*
dist/

# test binary, built with `go test -c`
*.test
# environment
.env
# output of the go coverage tool, specifically when used with LiteIDE
*.out
# output of the web benchmarking tool
*.pdf
# go workspace files
go.work*

# === IDE SETTINGS === #

# Visual Studio
.vs
# Visual StudioCode
.vscode
!tools/debug/.vscode/
# JetBrains
.idea

# === TASKFILE SETTINGS === #

# checksums
.task/

# === OBSIDIAN SETTINGS === #

# to exclude Obsidian's settings (including plugin and hotkey configurations)
.obsidian/
# to only exclude plugin configuration. Might be useful to prevent some plugin from exposing sensitive data
.obsidian/plugins
# OR only to exclude workspace cache
.obsidian/workspace.json
# to exclude workspace cache specific to mobile devices
.obsidian/workspace-mobile.json
# Add below lines to exclude OS settings and caches
.trash/
.DS_Store

# === DATA STORAGES === #

**/data/**
**/files
**/storage

# === LOGS === #

*.log
log/

# === DEPLOY === #

deploy/dgraph/acl/**

# Should be at the bottom of a file for better scope
!.gitkeep